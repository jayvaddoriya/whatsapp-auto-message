# Node dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Database (MongoDB is external, no local files to ignore)

# WhatsApp credentials & sessions (VERY IMPORTANT to prevent leak)
backend/auth/

# Production build outputs (compiled dynamically on hosting providers)
frontend/dist/

# Env files (containing local secrets)
.env
backend/.env
frontend/.env
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
